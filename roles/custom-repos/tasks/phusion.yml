---
  - name: Add phusion repo keys
    apt_key:
      id: 561F9B9CAC40B2F7
      keyserver: keyserver.ubuntu.com:80
      validate_certs: "{{ trust_repos }}"

  - mame: Add phusion repo
    apt_repository:
      repo: deb https://oss-binaries.phusionpassenger.com/apt/passenger xenial main
      state: present
      validate_certs: "{{ trust_repos }}"
